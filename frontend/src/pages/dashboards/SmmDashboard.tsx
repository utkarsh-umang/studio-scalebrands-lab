import { AppShell } from '@/components/AppShell'

export function SmmDashboard() {
  return (
    <AppShell title="Social Media Manager" subtitle="Pipeline">
      <div className="border-border bg-background rounded-xl border p-6">
        <p className="text-muted-foreground text-sm">
          Placeholder — clips, ideas, text, QA, and scheduling will connect
          here.
        </p>
      </div>
    </AppShell>
  )
}
